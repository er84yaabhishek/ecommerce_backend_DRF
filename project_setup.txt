python -m venv venv
venv\Scripts\activate

pip install django djangorestframework psycopg2-binary python-dotenv

django-admin startproject backend .
python manage.py startapp store




pip install django-cors-headers




DATABASE SETUP


net start postgresql-x64-18
"C:\Program Files\PostgreSQL\18\bin\psql.exe" -U postgres -h 127.0.0.1 -p 5433

postgres=# CREATE ROLE postgres WITH LOGIN PASSWORD 'admin';
ERROR:  role "postgres" already exists
postgres=# ALTER ROLE postgres SUPERUSER CREATEDB CREATEROLE;
ALTER ROLE
postgres=# CREATE DATABASE ecommerce_db OWNER postgres;
CREATE DATABASE
postgres=# \l   (to show all db list)

setting.py

from pathlib import Path
import os
from dotenv import load_dotenv
load_dotenv(BASE_DIR / ".env")


DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': os.getenv('DB_NAME'),
        'USER' : os.getenv('DB_USER'),
        'PASSWORD' : os.getenv('DB_PASSWORD'),
        'HOST' : os.getenv('DB_HOST'),
        'PORT' : os.getenv('DB_PORT'),
    }
}


python manage.py migrate

1️⃣ Project Structure (High Level)

E-COMMERCE
│
├── backend/                  ← Django (API)
│   ├── store/                ← App
│   │   ├── models.py
│   │   ├── views.py
│   │   ├── serializers.py
│   │   └── urls.py
│   │
│   ├── backend/
│   │   ├── settings.py
│   │   ├── urls.py
│   │   └── wsgi.py
│   │
│   └── manage.py
│
└── frontend/                 ← React
    ├── src/
    ├── components/
    └── App.jsx



===============================================================================
video 2

create models: Category , Product,UserProfile,Order,Order Item
use django Admin to add sample DATA 
create DRF Serializers for Product & Category
Expose/api/products/and /api/categories/endpoints
Tailwind CSS setup

2️⃣ Database Relation MAP (IMPORTANT)
User
 │
 ├── UserProfile
 │
 └── Order
       │
       └── OrderItem
              │
              └── Product
                      │
                      └── Category

3️⃣ Models ka Relation (Simple Words)

Category
 └── Products (many)

🟩 Product

Product
 ├── belongs to → Category
 └── used in → OrderItem

🟨 User

User
 ├── has one → UserProfile
 └── has many → Orders

🟥 Order

Order
 ├── belongs to → User
 └── has many → OrderItems

🟪 OrderItem

OrderItem
 ├── belongs to → Order
 └── belongs to → Product


User
 └── Order
       └── OrderItem
              └── Product
                     └── Category

===========================================================================
video 3

Frontend Product Listing page

fetch/api/products/using useEffect + fetch()
Display products in grid(image + name + price)
Add loading / error states
Tailwind styling
======================================================
video 4
Product Detail Page & Routing

Setup React Router DOM 
Create ProductDetail.jsx page 
Fetch /api/products/:id/
Display product image, description,price
Add to cart button (UI only)